services:
  mongodb:
    image: mongo:4.4
    container_name: recept-ocr-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
